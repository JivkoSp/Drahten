This directory contains DbInitializer class that has one purpose: Apply the entity framework migrations (the migrations are contained in the
Migrations directory that is on the same level as this directory) from all DbContext classes automatically when the application is run. 