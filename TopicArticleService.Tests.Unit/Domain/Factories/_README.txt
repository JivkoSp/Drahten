This directory contains unit tests for the domain entity factories.
-------------------------
Each class in this directory is related to a factory for domain entity that has the same name but without the Tests suffix.
For example the class ArticleFactoryTests contains unit tests related to ArticleFactory and so on.